import { HoraMinutoSegundoPipe } from './hora-minuto-segundo.pipe';

describe('HoraMinutoSegundoPipe', () => {
  it('create an instance', () => {
    const pipe = new HoraMinutoSegundoPipe();
    expect(pipe).toBeTruthy();
  });
});
